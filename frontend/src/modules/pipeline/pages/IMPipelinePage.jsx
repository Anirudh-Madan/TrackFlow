import PipelineBoard from '../components/PipelineBoard'

export default function IMPipelinePage() {
  return (
    <PipelineBoard
      title="Pipeline — Inventory Manager"
      subtitle="Approve admin-cleared orders, pick parts from stock, and assign a Dispatch Worker."
    />
  )
}
